# Copy to .env and fill in. Read by load_env_file() in main.py (lines 65-73),
# which does a plain split on the first "=" -- no quoting, no escaping, no
# export keyword. Write the value bare.
#
# DASHBOARD_PASS is the dashboard password; the username is CLIENT_NAME from
# user/credentials.py. If this is unset, dashboard.PASSWORD becomes None and
# nobody can log in.

DASHBOARD_PASS=
